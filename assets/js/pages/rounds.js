import {card,metric,pageHeader,sourceBadge} from "../components/ui.js";export function roundsPage(state){return `<div class="page">${pageHeader("GARMIN GOLF","Mine runder","Praktiske resultater fra banen.")}${state.rounds.map(r=>card(`<div class="row"><div>${sourceBadge("Garmin")}<h2 class="card-title">${r.course}</h2><p class="text-muted">${r.date}</p></div><div class="round-card__score">${r.score}</div></div><div class="metric-grid metric-grid--3">${metric("FIR",r.fir==null?"–":r.fir+"%")} ${metric("GIR",r.gir==null?"–":r.gir+"%")} ${metric("Putts",r.putts??"–")}</div>`)).join("")}</div>`}