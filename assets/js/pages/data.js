import {card,pageHeader,sourceBadge,metric} from "../components/ui.js";export function dataPage(state){return `<div class="page">${pageHeader("LOKAL BEHANDLING","Dataimport","Filer behandles i browseren og gemmes lokalt.")}${state.status?`<div class="status status--${state.status.type}">${state.status.text}</div>`:""}${card(`${sourceBadge("TrackMan")}<h2 class="card-title">TrackMan CSV</h2><p class="text-muted">Kolonner: Club, Carry, Total og Side Offline.</p><button class="button button--upload" id="trackmanButton">Vælg TrackMan CSV</button>`)}${card(`${sourceBadge("Garmin")}<h2 class="card-title">Garmin Golf</h2><p class="text-muted">JSON eller CSV med bane, dato, score, FIR, GIR og putts.</p><button class="button button--upload" id="garminButton">Vælg Garmin-fil</button>`)}${card(`<h2 class="card-title">Aktuelt datagrundlag</h2><div class="metric-grid">${metric("TrackMan",state.clubs.reduce((s,c)=>s+(c.shots||0),0)+" slag")}${metric("Garmin",state.rounds.length+" runder")}</div><button class="button button--outline button--full" id="resetButton">Gendan demodata</button>`,true)}<div class="status status--info">Gem aldrig Garmin-login eller tokens i GitHub-repositoriet.</div></div>`}